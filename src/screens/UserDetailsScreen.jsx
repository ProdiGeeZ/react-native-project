import React from 'react';
import { View, Text } from 'native-base'

const UserDetailsScreen = ({ routes, navigation }) => {
  console.log({ routes });
  return (
    <View>
      {/* <Text>{routes.params.id}</Text> */}
    </View>
  )

}
export default UserDetailsScreen; 